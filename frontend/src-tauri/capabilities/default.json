{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the ARES desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
